# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# local runtime data (file-backed deployment store)
/.data/

# env & secrets — never commit
.env
.env.local
.env*.local

# terraform
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example

# os / editor
.DS_Store
*.log
